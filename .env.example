# Gigent Configuration
PORT=3000

# Blockchain — Base (Ethereum L2)
# Default: Base Sepolia testnet. Set CHAIN=base for mainnet.
CHAIN=base-sepolia
# BASE_RPC_URL=https://mainnet.base.org
# BASE_SEPOLIA_RPC=https://sepolia.base.org

# Pimlico — ERC-4337 Bundler & Paymaster (required for Smart Accounts)
# 1. Create an account at https://dashboard.pimlico.io
# 2. Create a project, select "Base Sepolia" or "Base"
# 3. Paste your API key below
PIMLICO_API_KEY=

# Smart Contracts (deployed on Base Sepolia)
# Deploy with: cd contracts && npx hardhat run scripts/deploy.ts --network base-sepolia
REGISTRY_CONTRACT=
ESCROW_CONTRACT=
REVIEW_CONTRACT=
DEPLOYER_PRIVATE_KEY=

# On-chain escrow toggle (set to "true" to use PaymentEscrow contract)
# When false, uses centralized escrow wallet (backward compatible)
USE_ONCHAIN_ESCROW=false
